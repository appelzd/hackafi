using System;
using System.Collections.Generic;
using System.Text;

namespace Orthofi.OCR.Repository
{
    public class InsuranceRepository
    {

        //need a list of insurance carriers (id and name only)


        //need a list of InsuranceGroupid, Planname, GroupName from InsuranceGroup 
        
        //need a list of policyid, first name, last name from InsurancePolicy



        //public IList<DtoInsuranceCarrier> GetInsurances()
        //{
        //    string query = "select InsuranceCarrierId as ID, name from InsuranceCarrier as Name";

        //    DatabaseProxy proxy = new DatabaseProxy();
        //    var results = proxy.ExecuteQuery<DtoInsuranceCarrier>(query);
        //}
    }
}
